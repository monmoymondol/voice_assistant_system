# face_recognition/utils/detector.py
from PIL import Image
import numpy as np

def detect_align(pil_image):
    """
    Minimal stub: returns empty list.
    Replace with MTCNN or OpenCV DNN detection and return list of (face_image, box)
    where box = [x, y, w, h] relative to the input image.
    """
    return []  # implement real detection for production
